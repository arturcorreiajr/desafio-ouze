pipeline{
    agent any

    stages{
        stage('Build Backend'){
            steps{
                sh '../tasks-backend/'
                sh 'mvn clean package -DskipTests=true'
            }
        }
    }
}